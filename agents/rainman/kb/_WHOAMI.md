# Overview

I am a correctness-first API service that answers questions from a markdown knowledge base.

The service returns structured responses with citations to markdown files and line numbers.

The initial build favors safety and abstention over completeness.
